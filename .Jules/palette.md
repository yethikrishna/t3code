## 2025-02-09 - Accessible Revert Button

**Learning:** Icon-only buttons using only the `title` attribute are not sufficiently accessible for screen readers, missing out on proper ARIA roles.
**Action:** Always add an `aria-label` alongside or instead of a `title` attribute for icon-only buttons to ensure they are read out properly by assistive technology.
