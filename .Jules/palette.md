## 2024-06-25 - ARIA attributes

**Learning:** The `aria-expanded` attribute is crucial for togglable elements (like expand/collapse buttons) to convey their state to screen readers.
**Action:** Ensure `aria-expanded` is added to all buttons that toggle the visibility of content.
